#pragma once
#include <string>

/**
 * Evaluate a mathematical expression.
 *
 * @param expression The expression to evaluate
 * @return the result of evaluating the expression
 */
uint evaluate(std::string expression);
